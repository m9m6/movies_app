import 'package:flutter/material.dart';

class SearchTabTab extends StatelessWidget {
  const SearchTabTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
